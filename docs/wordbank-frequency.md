# Word frequency check

Generated by `python3 scripts/check-word-frequency.py`. Diagnostic only.

Zipf frequency over a mixed English corpus: 4 is an everyday word, 3 is known
but not frequent, below 1.5 is effectively not in use.

**This is a list to review, not a cut list.** Rap vocabulary skews away from
general English, so an uncommon word is not automatically a bad prompt —
`sycophant` sits at 2.2 and earns its place. The bottom of the range is the
unambiguous part: a word at 0.0 appears in the corpus essentially never.

## Tier 1 — 1223 words

- **1** below 1.5 (effectively not in use)
- **13** between 1.5 and 2.3 (rare; review by ear)

### Not in use

These are the clear faults — strings a suffix rule can build that no
writer will pick up. Removing them costs no vocabulary.

`parch` 1.4

### Rare — review by ear

`strew` 1.6 · `splay` 2.1 · `plunk` 2.1 · `laze` 2.2 · `flunk` 2.2 · `scald` 2.2 · `croon` 2.2 · `thresh` 2.2 · `strop` 2.2 · `clunk` 2.2 · `writhe` 2.2 · `mope` 2.2 · `plod` 2.3

## Tier 2 — 2035 words

- **0** below 1.5 (effectively not in use)
- **40** between 1.5 and 2.3 (rare; review by ear)

### Rare — review by ear

`turnback` 1.6 · `waylay` 1.6 · `starfall` 1.7 · `cufflink` 1.7 · `dreadlock` 1.7 · `deadfall` 1.7 · `carjack` 1.8 · `rashness` 1.8 · `chainlink` 1.8 · `deepness` 1.9 · `hubcap` 1.9 · `flashbulb` 1.9 · `setpoint` 1.9 · `slapshot` 1.9 · `anthill` 2.0 · `stolid` 2.0 · `backstab` 2.0 · `dropoff` 2.0 · `unctuous` 2.0 · `burnish` 2.0 · `chillout` 2.1 · `outplay` 2.1 · `sunstroke` 2.1 · `bookend` 2.1 · `deadbolt` 2.1 · `portend` 2.1 · `oxbow` 2.2 · `highpoint` 2.2 · `dumbstruck` 2.2 · `matchstick` 2.2 · `woodblock` 2.2 · `wrathful` 2.2 · `moonbeam` 2.2 · `graven` 2.2 · `affray` 2.2 · `blacktop` 2.2 · `blacken` 2.2 · `kickstand` 2.2 · `resound` 2.3 · `lampshade` 2.3

## Tier 3 — 2156 words

- **0** below 1.5 (effectively not in use)
- **98** between 1.5 and 2.3 (rare; review by ear)

### Rare — review by ear

`cummerbund` 1.7 · `airwave` 1.8 · `bandolier` 1.8 · `ceremonious` 1.8 · `stalagmite` 1.8 · `stalactite` 1.8 · `gabardine` 1.8 · `weaponization` 1.8 · `dilapidation` 1.8 · `coverall` 1.8 · `gondolier` 1.8 · `systematization` 1.9 · `thunderhead` 1.9 · `pallbearer` 1.9 · `systematize` 1.9 · `materialization` 1.9 · `jalopy` 1.9 · `negligee` 1.9 · `stevedore` 1.9 · `exasperate` 1.9 · `vainglorious` 1.9 · `crinoline` 1.9 · `monopolization` 1.9 · `seismograph` 1.9 · `sharecropper` 1.9 · `forcefield` 1.9 · `sidetrack` 2.0 · `calamine` 2.0 · `attune` 2.0 · `percolator` 2.0 · `instantiation` 2.0 · `overestimation` 2.0 · `emotionality` 2.0 · `familiarization` 2.0 · `sociality` 2.0 · `ferryboat` 2.0 · `interlace` 2.0 · `windowpane` 2.0 · `ziggurat` 2.0 · `macaroon` 2.0 · `immortalize` 2.1 · `vocalize` 2.1 · `dromedary` 2.1 · `finalization` 2.1 · `catacomb` 2.1 · `seafarer` 2.1 · `trebuchet` 2.1 · `nectarine` 2.1 · `arpeggio` 2.1 · `derringer` 2.1 · `loquacious` 2.1 · `barbiturate` 2.1 · `maracas` 2.1 · `organza` 2.1 · `pinafore` 2.1 · `dulcimer` 2.1 · `vermicelli` 2.1 · `racketeer` 2.1 · `humbleness` 2.1 · `weaponize` 2.1 · `denizen` 2.1 · `dirigible` 2.2 · `excommunicate` 2.2 · `overstock` 2.2 · `triangulate` 2.2 · `solidification` 2.2 · `tripwire` 2.2 · `cerebrum` 2.2 · `oleander` 2.2 · `worthlessness` 2.2 · `timpani` 2.2 · `recrimination` 2.2 · `cherubim` 2.2 · `pimento` 2.2 · `sycophant` 2.2 · `bodacious` 2.2 · `impale` 2.2 · `ferryman` 2.2 · `popularization` 2.2 · `dungarees` 2.2

…and 18 more.

